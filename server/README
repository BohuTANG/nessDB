Python server for nessDB
============================================================

Install ENV
------------------------------------------------------------
* install package `python-dev` if you use linux
* install tornado: https://github.com/facebook/tornado/ (or pip)

Install nessDB library
------------------------------------------------------------
* $ make install

Run
------------------------------------------------------------
* $ python server.py

